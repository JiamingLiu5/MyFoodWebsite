version: '3.8'
services:
  web:
    build: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./data.db:/app/data.db
    restart: unless-stopped
