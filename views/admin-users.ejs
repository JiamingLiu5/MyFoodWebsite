<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>User Management</title>
    <link rel="stylesheet" href="/static/style.css" />
  </head>
  <body>
    <div class="container">
      <header class="page-header">
        <div>
          <h1 class="brand-title">User Management</h1>
          <p class="brand-subtitle">Manage user roles and permissions</p>
        </div>
        <nav class="top-nav">
          <a href="/">Home</a>
          <a href="/admin/audit">Audit Log</a>
          <form class="top-nav-form" action="/logout" method="post">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
            <button class="top-nav-btn" type="submit">Logout</button>
          </form>
        </nav>
      </header>

      <% if (adminUserError) { %>
        <p class="upload-error"><%= adminUserError %></p>
      <% } %>
      <% if (adminUserMessage) { %>
        <p class="success-note"><%= adminUserMessage %></p>
      <% } %>

      <section class="users-list">
        <% if (users.length === 0) { %>
          <p>No users found.</p>
        <% } else { %>
          <table class="users-table">
            <thead>
              <tr>
                <th>ID</th>
                <th>Username</th>
                <th>Role</th>
                <th>Pin Right</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% users.forEach(function(user) { %>
                <% const hasPinRight = user.role === 'admin' || Number(user.can_pin || 0) === 1; %>
                <% const hasExplicitPinRight = Number(user.can_pin || 0) === 1; %>
                <tr>
                  <td><%= user.id %></td>
                  <td>
                    <%= user.username %>
                    <% if (user.id === userId) { %>
                      <span class="badge">(You)</span>
                    <% } %>
                  </td>
                  <td>
                    <span class="role-badge role-<%= user.role %>"><%= user.role %></span>
                  </td>
                  <td>
                    <span class="permission-badge <%= hasPinRight ? 'permission-on' : 'permission-off' %>">
                      <%= hasPinRight ? 'Can Pin' : 'No Pin' %>
                    </span>
                  </td>
                  <td class="user-actions">
                    <form class="inline-form reset-password-form" action="/admin/users/<%= user.id %>/reset-password" method="post">
                      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                      <input class="reset-password-input" type="password" name="newPassword" minlength="8" placeholder="New password (min 8)" required />
                      <button class="reset-btn" type="submit" onclick="return confirm('Reset password for <%= user.username %>?')">Reset Password</button>
                    </form>
                    <% if (user.id !== userId) { %>
                      <!-- Change role -->
                      <form class="inline-form" action="/admin/users/<%= user.id %>/role" method="post">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                        <input type="hidden" name="role" value="<%= user.role === 'admin' ? 'normal' : 'admin' %>" />
                        <button class="role-btn" type="submit">
                          <%= user.role === 'admin' ? 'Demote to Normal' : 'Promote to Admin' %>
                        </button>
                      </form>

                      <% if (user.role !== 'admin') { %>
                        <form class="inline-form" action="/admin/users/<%= user.id %>/pin-permission" method="post">
                          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                          <input type="hidden" name="canPin" value="<%= hasExplicitPinRight ? 0 : 1 %>" />
                          <button class="perm-btn" type="submit">
                            <%= hasExplicitPinRight ? 'Revoke Pin Right' : 'Grant Pin Right' %>
                          </button>
                        </form>
                      <% } else { %>
                        <span class="info-text">Admin always can pin</span>
                      <% } %>

                      <!-- Delete user -->
                      <form class="inline-form" action="/admin/users/<%= user.id %>/delete" method="post" onsubmit="return confirm('Delete user <%= user.username %>? All their posts will be deleted.')">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                        <button class="delete-btn" type="submit">Delete</button>
                      </form>
                    <% } else { %>
                      <span class="info-text">Current user (self reset allowed)</span>
                    <% } %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } %>
      </section>

      <div class="info-box">
        <h3>User Management Info</h3>
        <ul>
          <li><strong>Admin users</strong> can see all posts, create/edit/delete any post, pin/unpin posts, and manage users.</li>
          <li><strong>Pin right</strong> lets a non-admin user pin/unpin posts they can view without giving admin access.</li>
          <li><strong>Password reset</strong> allows admin to set a new password for any user, including self.</li>
          <li><strong>Normal users</strong> can create and manage their own posts, and view pinned content from others.</li>
          <li>You cannot delete or demote yourself.</li>
          <li>You cannot delete or demote the last admin user.</li>
        </ul>
      </div>
    </div>
    <script src="/static/script.js"></script>
  </body>
</html>
